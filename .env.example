DATABASE_URL="file:./dev.db"
# Signing secret for per-device password (each device sets its own password once). Leave unset for no protection.
APP_PASSWORD="any-secret-string"
# Or: COOKIE_SECRET="any-secret-string"
